---
description: 自动完成 feature_list.json 中所有未完成的功能，中途不询问
allowed-tools: Bash, Read, Write, Edit, MultiEdit, Glob, Grep, WebFetch
---

请按照以下指令执行，**全程不要停下来询问用户**：

## 执行步骤

### 初始化
1. 运行 `./init.sh` 启动环境
2. 读取 `claude-progress.txt` 了解历史进度
3. 读取 `feature_list.json` 获取完整功能列表

### 主循环（持续执行直到所有功能完成）

重复以下步骤，直到 feature_list.json 中没有 passes=false 的功能：

```
步骤 A: 选择任务
  → 读取 feature_list.json
  → 按 priority 排序（critical > high > medium > low）
  → 选择第一个 passes=false 的功能
  → 在终端打印：「⚙️ 开始实现: [功能名称] ([当前]/[总数])」

步骤 B: 实现功能
  → 分析需要修改/创建哪些文件
  → 直接实现，不询问
  → 如需安装依赖直接安装

步骤 C: 验证功能
  → 按照该功能 steps 字段逐步验证
  → 验证通过 → 执行步骤 D
  → 验证失败 → 修复 → 重新验证（最多重试 3 次）
  → 重试 3 次仍失败 → 将 passes 标记为 "blocked" 并记录原因 → 继续下一个功能

步骤 D: 提交进度
  → 将该功能 passes 改为 true
  → git add -A && git commit -m "feat: [功能描述]"
  → 追加更新 claude-progress.txt

步骤 E: 继续
  → 回到步骤 A
```

### 完成时
输出完整总结报告：
- ✅ 完成的功能列表
- ⚠️ blocked 的功能及原因
- 📊 总耗时估算
- 🔜 建议的后续步骤
